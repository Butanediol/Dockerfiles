kind: pipeline
name: build-image

steps:
  - name: build-server
    image: plugins/docker
    settings:
      username: 
        from_secret: docker_username
      password: 
        from_secret: docker_password
      registry: registry.cn-hangzhou.aliyuncs.com
      repo: axeler4t0r/code-server
      tags: ci